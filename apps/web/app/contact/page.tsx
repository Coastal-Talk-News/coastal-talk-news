import { ComingSoon } from '../../components/ui/ComingSoon';

export const metadata = { title: 'Contact' };

export default function ContactPage() {
  return (
    <ComingSoon
      title="Contact"
      description="Contact details are being set up. Reach the newsroom through the links in the footer for now."
    />
  );
}
